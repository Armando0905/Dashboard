version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip install Flask
    build:
      commands:
        - python app.py
  artifacts:
    baseDirectory: ""
    files:
      - app.py